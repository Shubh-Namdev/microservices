apiVersion: apps/v1
kind: Deployment
metadata:
  name: electronic-bazaar-order
spec:
  replicas: 1
  selector:
    matchLabels:
      app: electronic-bazaar-order
  template:
    metadata:
      labels:
        app: electronic-bazaar-order
    spec:
      containers:
        - name: electronic-bazaar-order
          image: shubhamnamdev01/electronic-bazaar-order-service:v1
          ports:
            - containerPort: 8084
          env:
            - name: SPRING_DATASOURCE_URL
              value: jdbc:mysql://mysql-db.cbk2awmcs7cz.ap-south-1.rds.amazonaws.com:3306/electronic_bazaar?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC              
              # value: jdbc:mysql://mysql:3306/electronic_bazaar?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: rds-db-cred
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: rds-db-cred
                  key: password
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: eb-app-secret
                  key: jwt_secret
            - name: PRODUCT_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: electonic-bazaar-config
                  key: product_service_url
            - name: CUSTOMER_SERVICE_URL
              valueFrom:
                configMapKeyRef:
                  name: electonic-bazaar-config
                  key: customer_service_url
---
apiVersion: v1
kind: Service
metadata:
  name: electronic-bazaar-order-service
spec:
  selector:
    app: electronic-bazaar-order
  ports:
    - port: 8084
      targetPort: 8084
  type: ClusterIP
